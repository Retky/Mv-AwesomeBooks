// Navigation
const navlist = document.querySelectorAll('.navlink');
const section = document.querySelectorAll('.section');
const bookSection = document.getElementById('bookList');

export {navlist, section, bookSection}
